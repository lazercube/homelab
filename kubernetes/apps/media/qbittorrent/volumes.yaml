apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-fast
  volumeName: pv-qbittorrent
  resources:
    requests:
      storage: 5Gi
